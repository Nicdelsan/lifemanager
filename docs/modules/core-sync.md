# :core:sync

**Scheletro vuoto da WP-0.1.** Backup/export/import cifrato e sync su Google
Drive (`appDataFolder`) sono responsabilità di WP-6.1 (Fase 6). Il file
placeholder `Sync.kt` esiste solo per soddisfare il vincolo di Konsist
"ogni layer deve contenere almeno un file".
